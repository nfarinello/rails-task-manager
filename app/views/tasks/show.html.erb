<h1><%= @task.title %></h1>
<%= @task.details %>
<%= @task.completed %>
<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
<%= link_to "Update", edit_task_path(@task) %>
